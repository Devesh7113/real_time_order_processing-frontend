/**
 * Matches backend `ProductCreateDTO` (admin create/update).
 *
 * @typedef {Object} ProductCreateDTO
 * @property {string} name
 * @property {string} description
 * @property {number} price
 * @property {number} stockQuantity
 * @property {number} [reservedQuantity] Listing/read-only; set by inventory when orders reserve stock.
 * @property {string} category
 * @property {string} imageUrl
 */

export {}
